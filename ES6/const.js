const CARS = ['BMW', 'AUDI'];

CARS.push('NANO')   // this is allowed even it is const because we are not
// changing the reference (like in variables), 
// we are just adding something to the array.
console.log(CARS);

